


<div class="row">
  <div class="col-sm-6">
    <h1>Suppliers</h1>
  </div>

  <div class="col-sm-6 text-right">
  <%= link_to new_supplier_path, class: 'btn btn-primary' do %>
    Add New Supplier
  <% end %>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
    
        <th>Name</th>
    

            <th>Location</th>
            <th>Full address</th>
            <th>Phone</th>
            <th>Type</th>
            <th>Production capacity</th>
            <th>Available</th>
            
      </tr>
    </thead>

    <tbody>
      <% @suppliers.each do |supplier| %>
        <%= content_tag :tr, id: dom_id(supplier), class: dom_class(supplier) do %>
          
            <td><%= link_to supplier.name, supplier %></td>
          

                      <td><%= supplier.location %></td>
                      <td><%= supplier.full_address %></td>
                      <td><%= supplier.phone %></td>
                      <td><%= supplier.type %></td>
                      <td><%= supplier.production_capacity %></td>
                      <td><%= supplier.available %></td>
          
          
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
